# cttimegate dependencies
#
# - The local dev server (serve.py) uses only the Python 3 standard library.
# - The GIF generator (capture.py) needs moderngl + Pillow.
#
# Tested with: Python 3.9+

moderngl>=5.10
Pillow>=10.0
numpy
